#include<stdio.h>
#include<math.h>
using namespace std;

void main()
{
  float sum=0.0f ;
  int i,n , c=1;
 
 cout<<"enter n value for series ";
 cin>>n;
 
 for(i=1;i<n;i++)
 { 
 sum = sum +  ( (float)(pow(c,2)) / (float)(pow(c,3)) );
		count+=2;
  }
  
  cout<<" Sum of the series :"<<sum;
  }
  
